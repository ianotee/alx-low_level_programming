#include <stdio.h>

/**
 * main - The printf will print character set.
 *
 * Return: 0 if (Successful).
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
