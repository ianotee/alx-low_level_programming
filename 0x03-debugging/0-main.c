#include "main.h"

/**
 * main - The main prints a positive or a negative integer.
 *
 * Return: 0 if (Successful).
 */
int main(void)
{
	int i;
	i = 0;

	positive_or_negative(i);
        return (0);
}
