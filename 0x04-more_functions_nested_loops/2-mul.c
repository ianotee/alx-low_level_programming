#include "main.h"

/**
 * mul - Function that multiplies two integers.
 *@a: integer param.
 *@b: interger param.
 * Return: 0 if(Successful).
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
